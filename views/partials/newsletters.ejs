<script>
  document.addEventListener('readystatechange', (evt) => {

   function subscribeNewsletters(email) {
    console.log("Ajax envoyé");
    $.ajax({
      url: "/subscribe_to_news_letters",
      type: "POST",
      data: {"email": email},
      contentType: "application/json",
      complete: () => {
        console.log("Reussi");
      },
      error: () => {
        console.log("Erreur serveur");
      }
    })
  
   }

  var btn_newsletters_form_submit = document.getElementById("btn_newsletters_form_submit");

  btn_newsletters_form_submit.addEventListener("click", (evt) => {
    var email = document.getElementById("newsletter_form_email").value;
    subscribeNewsletters(email);
  });

  });
 
</script>

<section class="positoin-relative z-index-9">
    <div class="container">
      <div class="row">
        
        <div class="col-lg-12">
          <div class="p-4 bg-primary border-radius">
            <div class="row">
              <div class="col-md-12">
                <%- include("./messages.ejs") %>
              </div>
            </div>
            <div class="row">
              <div class="col-md-12">
                <div class="row justify-content-between">
                  <div
                    class="col-lg-7 col-md-8 align-self-center mb-4 mb-md-0"
                  >
                    <h2>Newsletters</h2>
                    <p class="mb-0 text-white">
                      Inscrivez-vous a notre newsleters
                    </p>
                  </div>
                  <div
                    class="col-lg-5 col-md-4 justify-content-end align-self-center"
                  >
                    <div class="subscribe-form">
                      <form id="newsletters_form" action="/subscribe_to_news_letters" method="post">
                        <input
                          class="form-control" id="newsletter_form_email"
                          placeholder="Saisir votre email"
                          type="email" name="email"
                        />
                        <input type="button" id="btn_newsletters_form_submit"
                          class="animated4 btn btn-secondary mt-4 ml-1 ml-sm-2" value="S'inscrire">
                        
                      </form>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>